  <div class="col-xs-2">
  </div>
  <div class="container">
    <div class="row">
      <div class="col-xs-9">
       <h3>配送先登録/一覧</h3>
       <%= form_for(@address) do |a| %>
        <%= render 'shared/errors', object: @address %>
        <table class="table table-bordered">
         <tr>
           <th class="active">郵便番号</th>
　　　      <td><%= a.text_field :postal_code %></td>
         </tr>
         <tr>
           <th class="active">住所</th>
           <td><%= a.text_field :address %></td>
         </tr>
         <tr>
           <th class="active">宛名</th>
           <td><%= a.text_field :receiver_name %></td>
           <td>
            <%= a.submit "登録する", class: "btn btn-primary" %>
           </td>
         </tr>
        </table>
        
        
       <% end %>
    
       <table class="table table-bordered">

        <tr class="active">
          <th>郵便番号</th>
          <th>住所</th>
          <th>宛名</th>
          <th colspan="2">

          </th>
        </tr>
       <% @addresses.each do |a| %>
        <tr>
          <td><%= a.postal_code %></td>
          <td><%= a.address %></td>
          <td><%= a.receiver_name %></td>
          <td>
            <!-- <%# binding.pry %> -->
      	    <%= link_to"編集する", edit_address_path(a), class: "btn btn-primary" %>
            <%= link_to"削除する", address_path(a), method: :delete, class: "btn btn-danger" %>
          </td>
        </tr>
       <% end %>
      </table>
     </div>
    </div>
  </div>

